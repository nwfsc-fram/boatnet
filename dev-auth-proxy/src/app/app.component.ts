import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: 'Placeholder for auth server. See package.json'
})
export class AppComponent {
  title = 'dev-auth-proxy';
}
