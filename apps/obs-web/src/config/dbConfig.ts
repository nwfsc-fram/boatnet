import { DBConfig } from '@boatnet/bn-auth';

// Leave unchanged for Dev - overwritten in Prod
const dbConfig: DBConfig = {
  // apiUrl: 'https://localhost:9000'
  apiUrl: 'https://nwcdevmeow1.nwfsc.noaa.gov:9000'
};

export default dbConfig;
